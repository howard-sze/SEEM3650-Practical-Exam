Task 2:
The lige them sholds fater didor no one all eye.

KING RICHARD IIAN not what tey:
Thy the of have of heir befy off a cate whing ney
Plaction the worth my freses son the poon fall,
As with all not to say our thing have for the thuse seed

Task 3:

The lowest validation loss is 1.8895. The corresponding settings are layers = 3 and heads = 5.

Task 4:
train has 188,191 tokens
val has 20,911 tokens

first 20 lines:

self.pandas_sode():
                      "{'inspatcking is a prediction": Leas impon()
                                     )else:
                       "-module_notations : str
                    pest matering in a if titt stripppe docstrips ` itthat the tatils
            if tages tagenamer stimator

           .  versionary:
               newtith :repr
         self.get__testimator_tags()
                  )


    for asst.parsew(k, X.py)):
                 # The pred
---------------

    if _name initenal_dependennceptionsistency Docstript validates untficedur whin

favorite generated snippet:

elif self.estimator, yield() as = get_features_finit(res)
    arrgwe = True
    """
    yinstances_offf___out(contargs=1func_name, default=None, None,
           )


    out_clu == pos = 0

    exect_codes_outmerations_tow__glot_tags( prefines)
    ret = stoml_package])
